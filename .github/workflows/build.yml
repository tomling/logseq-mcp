name: Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
        
    - name: Restore dependencies
      run: |
        dotnet restore src/LogseqMcpServer/LogseqMcpServer.csproj
        dotnet restore tests/LogseqMcpServer.Tests/LogseqMcpServer.Tests.csproj

    - name: Build
      run: dotnet build src/LogseqMcpServer/LogseqMcpServer.csproj --no-restore

    - name: Test
      run: dotnet test tests/LogseqMcpServer.Tests/LogseqMcpServer.Tests.csproj --no-restore
